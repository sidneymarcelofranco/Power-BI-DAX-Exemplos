#Criar uma Função na Linguagem M para converter binário em uma imagem

let
    Conversor = (Fonte as binary) as text =>
    let
      Base64 = "data:image/png;base64, " & Binary.ToText(Fonte, BinaryEncoding.Base64)
     in
     Base64
in
   Conversor
